```
const list = [
    
    {id: 1, val: 1, parentId: 0},
    {id: 0, val: 0, parentId: 'root'},
    
    {id: 3, val: 3, parentId: 1},
    {id: 2, val: 2, parentId: 0},
    {id: 4, val: 4, parentId: 2}
]
//将该list转变为树形结构输出
[{
    id: 0,
    val: 0,
    parentId: 'root',
    children: [
        {
            id: 1,
            parentId: 0,
            children: [
                {
                    id: 3,
                    val: 3,
                    parentId: 1,
                    children: []
                }
            ]
        },
        {
            id: 2,
            val: 2,
            parentId: 0,
            children: [
                {
                    id: 4,
                    val: 4,
                    parentId: 2,
                    children: []
                }
            ]
        }
    ]
}]


function listTree(list,root='root'){
  let map = new Map()
  let tree = []
  list.forEach(item => {
    map.set(item.id,{...item,children:[]})
  })
  list.forEach(item => {
     if(item.parentId === root){
        tree.push(map.get(item.id))
     }else{
       let parent = map.get(item.parentId)
       parent.children.push(map.get(item.id))
     }
  })
  return tree
}

console.log(listTree(list,'root'))
```

