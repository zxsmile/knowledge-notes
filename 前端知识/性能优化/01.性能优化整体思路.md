## 为什么性能优化如此重要?

谷歌数据显示，页面加载时间每增加`1s` ，转化率就会降低`7%` ，用户满意率降低`16%`

#### 有完整的落地项目

- for dev：越来越卡？
- for customer：系统不好用
- for business：加载时间 加载时间每增加`1s` ，转化率就会降低`7%` ，用户满意率降低`16%`

#### `SEO`优化

- 搜索引擎优化是指通过优化网站的结构、内容和技术，提高网站在搜索引擎结果页面(如Google、百度等)的排名和流量
- 优化的目标是增加网站的曝光度，吸引更多的用户访问和流量
- performance LCP FID LCS

#### 网络环境

- `3G slow 4G`

#### 性能对用户的影响

- 影响用户的留存率
- 影响用户的转化率
- 品牌形象
- 用户满意度

## 从输入`url`到页面加载完成

![性能优化01](.\images\性能优化01.png)

## 性能指标

- `FCP`
- `LCP`
- `TTFB`
- `TTI`

## 性能指标的计算

#### `web-vitals`

```
import { onFCP, onLCP, TTFB} from 'web-vitals'

onFCP(console.log)
onLCP(console.log)
TTFB(console.log)
```

#### `performance api`

```
const entryHandler = (list) => {
  for(const entry of list.getEntries()) {
     if(entry.name === ' first-paint'){
        observer.disconnect()
     }
     //白屏时间
     let FP = entry.startTime
  }
}
const observer = new PerformanceObserver(entryHandler)
observer.observe({type:'paint', buffered:true})
```

```
const entryHandler = (list) => {
  for(const entry of list.getEntries()) {
     if(entry.name === ' first-contentful-paint'){
        observer.disconnect()
     }
     //首次内容绘制时间
     let FCP = entry.startTime
  }
}
const observer = new PerformanceObserver(entryHandler)
observer.observe({type:'paint', buffered:true})
```

## 问题分析和定位



## 性能检测工具

- `lighthouse`
- `performance`
- `Network`

## 网络层面优化

- 使用缓存

#### 浏览器缓存

- `SW`
- `HTTP Cache`

#### `DNS` 优化

- 减少`DNS`查询次数，提前解析关键域名，提升页面加载速度

- `DNS`预取（`DNS Prefetching`）

  ```
  <link rel="dns-preftch" href="//cdn.example.com">
  ```

- 避免过度预解析

  ```
  <link rel="preconnect" href="//cdn.example.com">
  ```

  进一步建立`TCP`连接，`TLS`握手

一般3-5个关键域名

#### 域名收敛

- 减少查询次数
- 利用`http2.0`多路复用特性，避免重复`TCP`握手

#### `CDN`

#### `SSR` (网络 + 渲染)

## 渲染层面

- 尽可能减少资源个数
- 尽可能减少资源体积大小

#### 减小文件大小

- 压缩`html`减少`html`体积
- `css unocss` 按需引入`css`原子能力
- `JS` => 学习不同构建工具 `webpack` `vite` `swc` `repack` `esbuild` `rollup` =>多个工具结合=>`vite + esbuild rollup`、 `webpack + swc => 平替babel swc-loader +rspack` => 优化`js`体积

#### 图片加载

