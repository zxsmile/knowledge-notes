## 架构级优化

#### 代码分割

- 路由级别

  ```
  const Home = lazy(() => import('./Home'))
  const About = lazy(() = import('./About')) //import 机制 原理
  
  <Suspense>
    <Routes>
       <Route path="/" element={<Home/>} />
       <Route path="/about" element={<About/>} />
    </Routes>
  </Suspense>
  ```

- 组件级别

  ```
  const HeaComponent = lazy(() => 
  import('./HeaComponent').then(module => ({
    default:module.HeaComponent
  }))
  )
  ```

#### 服务端渲染 `SSR`

- 首屏渲染
- 搜索引擎优化

同构

流式 `next.js`

 