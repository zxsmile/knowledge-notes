# 一、字面量类型

`Ts`支持直接使用字面量作用类型使用。

#### `Boolean`:

   `true`

   `false`

#### `String`:

​    直接使用字符串或者模板字符串

#### 数字字面量:`number`、`bigInit`

数值常量

```
const a: true = true
const b: false = false
let c: boolean;
c=a
c=b;
const str: '1'='1'
const strl:`2` = '2'
let string: string;
string = str
string = str1;
const num1: 1=1;
const num2:10n =10n;
let number: number;
let bining: bigint;
number = num1;
bining = num2;
```

## 枚举成员字面量类型

我们可以将枚学成员字面量类型表示

```
enum Dir{
Up,
Down,
Left,
Right
}

const up: Dir.Up = 0
const Down: Dir.Down = Dir.Down
```

# 