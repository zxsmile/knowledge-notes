# 一、单元类型

叫作单例，仅包含一个可能值的类型。由于特殊性质，所以`ts`编译器在处理单元类型时，不再去关注具体值。

#### 单元类型有几种:

- undefined
- null
- unique symbol
- void
- 字面量类型
- 枚举成员类型

```
const a:undefined = undefined
const b:null =null
const c:1 =1
const d:void = undefined
enum Foo{A,B}
const E:Fob.A =Foo.A
```

